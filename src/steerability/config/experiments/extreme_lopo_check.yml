probe: "./training_probes/steerability_2d_in_3d_128x8_evaluation_test.csv" # yes, probe is incorrectly named
prompt_strategy: "direct"
llm_settings:
    llm_name: "steer-lora-xtreme"
    chat_type: "vllm"
    cache_file: "vllm-meta-llama3-8b-lora.tsv"
    other_kwargs:
        port: 16384 
        max_simul_calls: 256 # 256 concurrent is the max we seem to be able to handle (max_simul_calls * num_generations)
        timeout: 6000 # WHAT THE
        text_gen_kwargs: # in sammo-language
            randomness: 0.0
            num_generations: 1
inst_addons:
    disambig: True
experiment_name: "llama3.1_8b_2d_in_3d_train_4xlopo_xtreme"
seed: 42
